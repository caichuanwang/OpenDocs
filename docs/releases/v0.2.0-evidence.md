# OpenDocs v0.2.0 Alpha 发布证据

Candidate commit: e0a8101aece0d2c52fe94c9799bfb734d2e21223

- 版本：`opendocs-sdk==0.2.0`
- 验收边界：XLSX 支持的精简 Alpha
- 候选提交：`92e2c73`（已推送至 `master`）

## 已通过的自动门

| 门 | 结果 |
| --- | --- |
| 公共 pytest | 824 passed, 9 skipped, 4 expected warnings |
| Ruff lint / format | PASS |
| ty | PASS |
| `uv sync --all-groups --frozen` | PASS |
| `uv build` | PASS |
| wheel/sdist artifact checker | PASS |
| Python 3.13 隔离 wheel 安装 | PASS |
| 隔离 wheel native smoke | `async/docx/markdown/pdf/pptx/text/xlsx=pass` |
| PR CI（Python 3.11、3.13、lint、build） | PASS，代码候选 PR #8 |

## XLSX 验收范围

- path、bytes、binary stream 以及 `parse()` / `aparse()` 对等。
- worksheet/chartsheet 按源顺序保留，包括 visible、hidden、very hidden 和空 sheet。
- 合并单元格、Excel table、相离区域、公式缓存回退、常见货币/日期/时间格式和标准文本对象均有合成回归。
- ChartML 原生标题、分类、系列、数值、literal 数据和组合图层均保留；图片和图表视觉增强 fail-open。
- ZIP/XML/关系安全、资源预算、取消、超时、worker 硬终止和临时文件清理均有自动化回归。

## 未运行或尚未完成

- 维护者真实 XLSX：`not_run`。计划允许其作为发布前探索门，不得把它写成真实兼容性已验证。
- live vision provider：未运行；合成 fake vision 覆盖视觉降级和合并语义。
- Windows：未验证。
- TestPyPI、PyPI、公共索引安装 smoke 和 GitHub Release：由 `v0.2.0` 标签工作流执行。
- 当前仓库不声明生产就绪、性能 SLA 或完整 Excel 像素级外观保真。

## 隐私与声明边界

本证据不包含真实工作簿、私有 hash、模型 payload、原始 Markdown、凭据或完成的私有检查表。
XLSX 不访问外部 URL、链接工作簿或数据连接；视觉请求仅在调用方配置 provider 时发生。
