# OpenDocs 0.1.0 Alpha

首个公开 Alpha 将本地 TXT、Markdown、PDF、图片、DOCX 和 PPTX 转换为 Markdown，并提供
稳定的同步/异步入口、类型化错误和有界解析选项。

发布阻断平台为 Ubuntu 与 macOS 的 Python 3.11-3.13。视觉路径可能调用使用者配置的
模型并产生费用；Windows 尚未验证。本版本不声明生产就绪、性能 SLA、全局并发或模型
费用上限。

完整兼容性和限制见 `CHANGELOG.md`，聚合质量与资源结果见随候选冻结的
`v0.1.0-evidence.md`。
